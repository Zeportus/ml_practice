run with "streamlit run app.py"
replace "roboflow_api_key" in detector.py with your key (its free by now).

detections directory contains frames with detection. Name of this detection consist of video name or webcam_frame and detection time.
csv report shows filenames with detection.
